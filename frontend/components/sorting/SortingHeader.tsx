import styles from '../../styles/Sorting.module.scss';

export const SortingHeader = () => {
    return(
        <div className={styles['header']}>
            {/* Add visual stats here */}
        </div>
    )
}