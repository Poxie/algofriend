import styles from '../../../styles/Home.module.scss';

export const HomeHeaderText = () => {
    return(
        <h2 className={styles['header-text']}>
            Algorithms are difficult, but they don't have to be.
        </h2>
    )
}